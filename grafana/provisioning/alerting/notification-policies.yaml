apiVersion: 1

# #188: 기본 정책 — 아직 다른 알림이 없으므로 전체를 Discord로 라우팅
policies:
  - orgId: 1
    receiver: discord-alerts
    group_by: ['alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 6h
